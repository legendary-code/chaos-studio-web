/** @jsx React.DOM */
var React = require('react');

var Menu = React.createClass({
    render: function () {
        return <div className="menu">menu</div>
    }
});

module.exports = Menu;